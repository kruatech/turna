// NOT the source of truth — do not edit or add messages here.
//
// The admin client compiles the canonical management contract directly from
// crates/control/proto/management.proto (see services/admin/build.rs). This
// stub remains only so a stale, drifting duplicate cannot be compiled by
// mistake; scripts/check-deploy-consistency.sh enforces that build.rs uses the
// control proto. Edit the contract in crates/control/proto/management.proto.
syntax = "proto3";

package turna.management.v1;
